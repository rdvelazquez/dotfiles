[user]
	name = rdvelazquez
	email = ryan.velazquez@newfront.com

[alias]
  co = checkout
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
  s = status
  d = difftool
  gone = ! "git fetch -p && git for-each-ref --format '%(refname:short) %(upstream:track)' | awk '$2 == \"[gone]\" {print $1}' | xargs -r git branch -D"

[diff]
	tool = vimdiff

[difftool]
	prompt = false

[github]
    name = rdvelazquez
    email = ryan.velazquez@newfront.com

[core]
	editor = /usr/bin/vim
	autocrlf = false
[http]
	sslVerify = false
[credential]
	helper = osxkeychain
